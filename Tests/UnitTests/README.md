# Tests/UnitTests

Unit tests using Swift Testing (`@Test`/`#expect`), mirroring the `Features/<FeatureName>/ViewModels` and `Core/*` structure they cover. Force-unwrap/force-try are permitted here (excluded in `.swiftlint.yml`).
