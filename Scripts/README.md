# Scripts

CI helpers, fastlane config (`Fastfile`/`Appfile`), and the XcodeGen/Tuist project-generator config land here at bootstrap time, based on the `toolchain.projectGenerator` and `ci.target` answers recorded in `.claude/project.json`.
