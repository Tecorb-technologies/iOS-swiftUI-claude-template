# Core/DesignSystem

Shared, reusable SwiftUI components, colors, typography, and spacing tokens used across features. If a Figma design source is recorded in `.claude/project.json` (`design.figmaFileUrl`), tokens here should trace back to it.
