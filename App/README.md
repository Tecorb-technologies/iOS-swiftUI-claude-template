# App

The SwiftUI app entry point lands here at bootstrap time: `<AppName>App.swift` (the `@main` struct), tagged `// GENERATED-BY-BOOTSTRAP`. Run `/bootstrap-ios` (or let the `tecorb-ios-bootstrap` skill auto-trigger) before adding anything here.
