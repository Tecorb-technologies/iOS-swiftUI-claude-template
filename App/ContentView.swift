// GENERATED-BY-BOOTSTRAP
import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack(spacing: 12) {
            Image(systemName: "checklist")
                .font(.largeTitle)
            Text("My ToDo App")
                .font(.title2.bold())
            Text("Bootstrapped. Ready for the first feature.")
                .font(.subheadline)
                .foregroundStyle(.secondary)
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
